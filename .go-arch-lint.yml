version: 3
workdir: internal
allow:
  depOnAnyVendor: true

components:
  adapter: { in: adapter/** }
  application: {in: application/** }
  core: { in: core/application/** }
  domain: { in: core/domain/** }
  presenter: { in: presenter/** }
  pkg: { in: ../pkg/** }

deps:
  application:
    mayDependOn:
      - application
      - adapter
      - core
      - domain
      - pkg
      - presenter
  presenter:
    mayDependOn:
      - core
      - domain
      - presenter
      - pkg
  core:
    mayDependOn:
      - adapter
      - core
      - domain
      - pkg
  adapter:
    mayDependOn:
      - domain
      - pkg
  domain:
    mayDependOn:
      - domain
      - pkg
